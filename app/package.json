{
  "name": "shkola-remonta-app",
  "version": "1.0.0",
  "description": "Визуализация проекта Виртуальная Школа Ремонта",
  "scripts": {
    "build": "node scripts/build-data.js",
    "serve": "cd public && python3 -m http.server 8080",
    "dev": "npm run build && npm run serve"
  }
}
